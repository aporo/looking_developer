<h1>Ranking types</h1>

<table>
  <tr>
    <th>Type</th>
    <th>Count</th>
  </tr>

<% @types.each do |type| %>
  <tr>
    <td><%= type[:name] %></td>
    <td><%= type[:count] %></td>
  </tr>
<% end %>
</table>

<br />
